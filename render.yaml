services:
  - type: web
    name: websocket-app # Replace with your desired name
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PORT
        value: "8001" # Or whatever port your app listens on
